{
    "name": "multi-service-app",
    "version": "1.0.0",
    "scripts": {
      "start": "cd frontend && npm start && cd ../vcall && npm start && cd ../backend && nodemon index.js",
      "dev" : "concurrently \"cd frontend && npm start\" \"cd vcall && npm start\" \"cd backend && npm start\""
    },
    
    "devDependencies": {
      "concurrently": "^6.2.2"
    }
  }

  
  